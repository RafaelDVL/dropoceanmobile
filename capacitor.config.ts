import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.dropoceanmobile',
  appName: 'dropoceanmobile',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
